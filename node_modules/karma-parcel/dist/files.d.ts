export interface IFile {
    dir: string;
    name: string;
    path: string;
    write: (str: string) => Promise<any>;
}
declare class TmpFile implements IFile {
    dir: string;
    name: string;
    path: string;
    private done;
    constructor(dir: string, name: string);
    write(content: string): Promise<void>;
}
export declare function createBundleFile(): TmpFile;
export declare class EntryFile extends TmpFile {
    private files;
    constructor(dir: string);
    add(path: string): Promise<void>;
    private importPath;
    toString(): string;
}
declare class Workspace {
    dir: string;
    bundleFile: string;
    entryFile: EntryFile;
    constructor(dir: string);
    toString(): string;
}
export declare function createWorkspaceSync(): Workspace;
export {};
